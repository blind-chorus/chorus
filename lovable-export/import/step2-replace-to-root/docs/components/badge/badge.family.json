{
  "$schema": "../../family.schema.json",
  "family": "badge",
  "name": "Badge",
  "description": "Numeric count indicator. Attaches to a host label (channel name, list row, thumbnail corner) to surface the unread or update count for that target. Single-spec family.",
  "useCases": [
    "unread count",
    "update count",
    "numeric indicator",
    "host attachment"
  ],
  "spec": "badge.md",
  "subcomponents": [
    {
      "slug": "badge",
      "spec": "badge.spec.json",
      "md": "badge.md",
      "default": true
    }
  ]
}

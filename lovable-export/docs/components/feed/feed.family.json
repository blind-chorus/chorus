{
  "$schema": "../../family.schema.json",
  "family": "feed",
  "name": "Feed",
  "description": "The scrolling-stream card family. Two shapes ride the same column — `default` (the authored content card: channel header, body block, optional inline modules, engagement footer) and `ad` (the in-feed sponsored placement: brand row, optional headline + body, a hero + CTA slab, no engagement row). Both share the same container surface, padding rung, and column-width cap so authored posts and sponsored placements tile cleanly side by side.",
  "useCases": [
    "content stream",
    "authored posts",
    "comments stream",
    "discussion thread",
    "timeline",
    "sponsored post",
    "in-feed ad",
    "promotion card",
    "brand placement"
  ],
  "spec": "feed.md",
  "subcomponents": [
    {
      "slug": "default",
      "spec": "default.spec.json",
      "md": "default.md",
      "default": true
    },
    {
      "slug": "ad",
      "spec": "ad.spec.json",
      "md": "ad.md"
    }
  ]
}

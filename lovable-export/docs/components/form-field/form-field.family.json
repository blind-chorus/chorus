{
  "$schema": "../../family.schema.json",
  "family": "form-field",
  "name": "FormField",
  "description": "Single-line text-entry family. `input` is the default labeled text field with optional helper / count; `search` is a bare pill-shaped variant with a leading search glyph; `select` is the Input-shaped picker that opens a sheet.",
  "useCases": [
    "single-line text input",
    "labeled text field",
    "search input",
    "bare search pill",
    "sheet-driven option picker",
    "labelled select+input pairing (e.g. country code + number)"
  ],
  "spec": "form-field.md",
  "subcomponents": [
    {
      "slug": "input",
      "spec": "input.spec.json",
      "md": "input.md",
      "default": true
    },
    {
      "slug": "search",
      "spec": "search.spec.json",
      "md": "search.md"
    },
    {
      "slug": "select",
      "spec": "select.spec.json",
      "md": "select.md"
    }
  ]
}

# @blind-chorus/icons

> Planned. Not yet implemented.

This package will hold the Chorus icon set — SVG sources plus a JSON manifest describing each icon's name, category, and usage notes. Consumed by the docs site and the Figma plugin alongside `@blind-chorus/tokens` and `@blind-chorus/components`.

Until the first icon lands, this folder is intentionally empty.

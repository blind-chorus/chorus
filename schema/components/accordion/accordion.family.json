{
  "$schema": "../../family.schema.json",
  "family": "accordion",
  "name": "Accordion",
  "description": "Vertical stack of expandable rows. Each item exposes a trigger header (label + auto-rendered trailing chevron that rotates 180\u00b0 on expand) and a content body that paints below it when open. Two modes select how many items can be open at once: `type='single'` (one open at a time \u2014 collapsible by default) and `type='multiple'` (any number open). Rows tile flush with a hairline `outlineVariant` divider between them. Single-spec family.",
  "useCases": [
    "FAQ block",
    "terms-of-service / policy sections",
    "expandable filter group",
    "settings group with infrequent edits",
    "long-form content carved into scannable sections"
  ],
  "visualReuse": "open",
  "layoutInset": "full-bleed",
  "wrapperGuidance": "Owns its inline padding internally. Place as a direct child of the page-shell <main> (or any host that pays the gutter once). Do NOT wrap in a padding-inline div, className=\"px-*\", or style={{ padding }} \u2014 the page rail is paid once at the shell, never on the full-bleed child. Inside a bounded surface (Dialog / BottomSheet / SideSheet), apply the negative-margin opt-out \u2014 see AGENTS.md \u00a7 Composition rules.",
  "spec": "accordion.md",
  "subcomponents": [
    {
      "slug": "accordion",
      "spec": "accordion.spec.json",
      "md": "accordion.md",
      "default": true
    }
  ]
}
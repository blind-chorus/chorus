{
  "$schema": "../../spec.schema.json",
  "name": "List",
  "family": "list",
  "subcomponent": "radio",
  "description": "Single-select picker List variant. Each row carries a leading 16px radio indicator; clicking any row commits its value via `onChange(value)`. Exactly one row is selected at a time (or zero before the user picks). Use inside picker sheets, settings rows, sort-by menus.",
  "element": "ul",
  "props": {
    "items": { "type": "node", "required": true, "description": "Array of row descriptors." },
    "value": { "type": "string", "optional": true, "description": "Selected value. Controlled." },
    "onChange": { "type": "function", "optional": true, "description": "Commits the next value when a row is selected." }
  },
  "slots": {
    "container": { "required": true, "description": "Outer scroll surface. Vertical stack with a transparent fill (inherits parent container tone); rows separated by a 1px outlineVariant divider, not a gap." },
    "row": { "required": true, "description": "Single list item. Whole row is the interactive target." },
    "leading": { "required": true, "description": "16px radio indicator — `RadioIcon` at rest, `RadioFillIcon` when selected. Decorative — keyboard focus and click target both sit on the row.", "accepts": ["icon"] },
    "label": { "required": true, "description": "Primary row text. 16px / Regular / onSurface. Single line; truncates with ellipsis.", "accepts": ["text"] },
    "supportingText": { "required": false, "description": "Secondary line under label. 14px / Regular / onSurfaceVariant, 2px below label.", "accepts": ["text"] }
  },
  "rowProps": {
    "value": { "type": "string", "required": true },
    "label": { "type": "string", "required": true },
    "supportingText": { "type": "string", "optional": true },
    "disabled": { "type": "boolean", "default": false },
    "forcedState": { "type": "literal", "values": ["hovered", "pressed", "focused"], "optional": true, "description": "Docs-only — pins the row to a single visual state via `data-force-state`. Not for production use." }
  },
  "sizing": {
    "rowPaddingBlock": "12px",
    "rowPaddingInline": "16px",
    "rowMinHeight": "48px",
    "leadingLabelGap": "sys.layout.inline.md",
    "dividerWidth": "sys.borderWidth.hairline",
    "dividerColor": "sys.color.outlineVariant",
    "labelTypo": "16 / Regular",
    "labelColor": "sys.color.onSurface",
    "supportingTypo": "14 / Regular",
    "supportingColor": "sys.color.onSurfaceVariant",
    "supportingOffset": "2px",
    "leadingRadioSize": "16 × 16",
    "leadingRadioColorRest": "sys.color.outline",
    "leadingRadioColorSelected": "sys.color.primary"
  },
  "states": {
    "default": { "overlay": null },
    "hovered": { "overlay": { "color": "label", "opacity": "sys.state.hover" } },
    "pressed": { "overlay": { "color": "label", "opacity": "sys.state.pressed" } },
    "selected": { "leading": "Filled primary indicator; row foreground stays at onSurface. No fill change on the row itself." },
    "disabled": { "containerOpacity": "sys.state.disabled", "pointerEvents": "none", "note": "Radio indicator dims with the row." }
  },
  "focusIndicator": {
    "description": "Keyboard-focus visual — an accessibility indicator, not a lifecycle state. The ring sits on the row, not on the leading indicator — the row is the keyboard target.",
    "composition": "inward",
    "compositionReason": "Rows tile the column flush with only a hairline `outlineVariant` divider between them; an outward ring would overlap the divider and the neighbouring row.",
    "overlay": { "color": "label", "opacity": "sys.state.focus" },
    "ring": {
      "outerWidth": "sys.borderWidth.thin",
      "outerColor": "sys.color.focus",
      "outerLayerPosition": "depth 0..2px from the row edge (the outer stroke)",
      "insetWidth": "sys.borderWidth.hairline",
      "insetColor": "sys.color.focusInset",
      "insetLayerPosition": "depth 2..3px from the row edge (the counter-ring just inside the outer stroke)",
      "implementation": "inset box-shadow on the row's `::before` overlay (the `::after` carries the inter-row divider). Constrained strictly inside the row's footprint and never exceeds it."
    },
    "trigger": ":focus-visible (keyboard / programmatic focus, never plain mouse click)"
  },
  "behavior": {
    "keyboardNavigation": "Arrow up/down moves focus between rows. Space and Enter commit the focused row's value. Home and End jump to first / last.",
    "selectionModel": "Single-select only; selecting a row implicitly deselects the previously selected. Controlled — pass `value` and `onChange`; uncontrolled use defaults to no selection.",
    "rowClickTarget": "Whole row is clickable. The leading indicator is never a separate hit target.",
    "truncationNotWrap": "Both label and supportingText truncate; the row never grows to fit long text."
  }
}

{
  "$schema": "../../family.schema.json",
  "family": "list",
  "name": "List",
  "description": "Vertical list of rows. Four sub-components share one anatomy and diverge on the leading slot and the selection contract: `text` (display / navigation rows), `radio` (single-select picker with leading 16px radio indicator), `thumbnail` (avatar-anchored rows with leading 40px Thumbnail), `nav` (drill-in rows with auto-rendered trailing chevron).",
  "useCases": [
    "settings rows",
    "menu rows",
    "picker rows",
    "single-select option group",
    "drill-in navigation",
    "avatar-anchored rows",
    "directory rows"
  ],
  "spec": "list.md",
  "subcomponents": [
    {
      "slug": "text",
      "spec": "text.spec.json",
      "md": "text.md",
      "default": true
    },
    {
      "slug": "radio",
      "spec": "radio.spec.json",
      "md": "radio.md"
    },
    {
      "slug": "thumbnail",
      "spec": "thumbnail.spec.json",
      "md": "thumbnail.md"
    },
    {
      "slug": "nav",
      "spec": "nav.spec.json",
      "md": "nav.md"
    }
  ]
}

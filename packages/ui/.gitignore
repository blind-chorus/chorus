# Everything under agents/ is regenerated by scripts/copy-agents.mjs at
# prepublish from the chorus root + schema/. Authored sources live at the
# repo root (AGENTS.md, prompt/LOVABLE.md) and under schema/; nothing under
# agents/ is hand-edited.
agents/

# Copied at prepublish from apps/docs/public/. The universal Chorus
# image-area placeholder, shared across every empty image slot.
placeholder.png

# Build artifacts.
dist/

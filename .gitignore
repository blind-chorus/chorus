node_modules/
**/.DS_Store
.env
.env.local
.env.*.local

# Next.js
.next/
out/

# Build artifacts (regenerated; ship via npm, not git)
packages/ui/dist/

# Playwright transients
apps/*/test-results/
apps/*/playwright-report/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/

# OS
Thumbs.db

# Local-only Claude Code memory symlink (points to user-specific path)
claude-memory

# lovable-export uses bun.lock (managed by the Lovable repo, not chorus).
# Guard against `npm install` inside lovable-export/ regenerating an npm lock
# that would then drift from Lovable's bun-managed dependency state.
lovable-export/package-lock.json
